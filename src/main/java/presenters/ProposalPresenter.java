package presenters;

//TODO implementa come si deve usando service
public class ProposalPresenter {
/*
	private ProposalView proposalView;
	private AbstractJpaProposalRepository abstractJpaProposalRepository;
	private AbstractJpaContractRepository abstractJpaContractRepository;

	public ProposalPresenter(ProposalView proposalView, AbstractJpaProposalRepository abstractJpaProposalRepository, AbstractJpaContractRepository abstractJpaContractRepository) {
		this.proposalView = proposalView;
		this.abstractJpaProposalRepository = abstractJpaProposalRepository;
		this.abstractJpaContractRepository = abstractJpaContractRepository;
	}

	public void showAllProposals(League actualLeague, FantaTeam myTeam) {
		proposalView.showAllProposals(abstractJpaProposalRepository.getMyProposals(, actualLeague, myTeam));
	}
	
	public void addProposal(League actualLeague, Player player1, Player player2) {
		Proposal proposal = abstractJpaContractRepository.getProposal(actualLeague, player1, player2);
		proposalView.addProposal(proposal);
	}

	public void acceptProposal(Proposal proposal) {
		abstractJpaProposalRepository.acceptedProposal(, proposal);
		proposalView.acceptProposal(proposal);
	}
	
	public void rejectProposal(Proposal proposal) {
		abstractJpaProposalRepository.rejectedProposal(, proposal);
		proposalView.rejectProposal(proposal);
	}

 */
}
